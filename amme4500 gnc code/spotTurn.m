function [outputCmd] = spotTurn()
%   This function returns the outputCmd that will help the rover turn on
%   the spot

% Since this is a spot turn we really only need to activate the steering
% motors

